name: pos_mvp
description: Point of Sale App

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Database
  sqflite: ^2.3.0
  path: ^1.8.3

  # PDF
  pdf: ^3.10.7
  printing: ^5.12.0

  # Excel
  excel: ^4.0.2

  # CSV
  csv: ^6.0.0

  # File saving & sharing
  path_provider: ^2.1.2
  share_plus: ^9.0.0
  permission_handler: ^11.3.0
  open_filex: ^4.3.4

  # Date & formatting
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
